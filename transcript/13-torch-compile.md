# 13 · torch.compile

Fusing the kernels you were about to launch.

Source: `src/pages/ch/13-torch-compile.mdx` · `src/components/TorchCompile.svelte`
Generated by `node scripts/transcript.mjs` — edit the source, not this file.

## Step 1 of 11

**Caption.** Chapter 12 left a piece looking like this: a chain of kernels between two matmuls, each one reading its input from memory and writing its output back. Take the two in the middle.

**On stage.**

- one piece from chapter 12 · the MLP end of a layer
- HBM
- gate·up
- silu
- mul
- down
- these two
- the two matmuls have to touch memory: that is where the weights are
- the two in the middle touch it only because of how they were called

## Step 2 of 11

**Caption.** SiLU writes 28 KB per token per layer into HBM, and multiply reads it back a microsecond later. Nothing else ever reads it. Across 32 layers that is 1.8 MB per token, out to memory and straight back.

**On stage.**

- the value between them · one token, one layer
- silu
- mul
- HBM
- write
- 28 KB
- [1 × 14,336]
- read back
- 28 KB
- nothing else ever reads it
- 56 KB of traffic per token per layer, written and read back within about a microsecond
- across 32 layers, 1.8 MB per token

## Step 3 of 11

**Caption.** It exists because eager PyTorch runs one line at a time. silu(gate) is a call, a call returns a tensor, and a tensor lives in HBM. Line 2 has not been read yet, so nothing knows the result is consumed next.

**On stage.**

- why the value is written down · eager runs one line at a time
- what you wrote
- h = silu(gate)
- h = h * up
- not read yet
- y = down(h)
- not read yet
- what the interpreter knows
- line 1 is a call, and a call has to return
- it returns a tensor, and a tensor lives in HBM
- line 2 is unread: nothing knows h is used next
- HBM
- silu returns h
- 28 KB
- h · [1 × 14,336]
- fusing needs both lines at once; when line 1 runs, the interpreter has only ever seen line 1

## Step 4 of 11

**Caption.** Fuse them. One kernel: load gate and up once, compute the SiLU and the multiply in registers, store the answer once. Identical arithmetic, two fewer trips through memory.

**On stage.**

- the same arithmetic, in one kernel
- HBM
- before
- 4 trips
- silu
- mul
- after
- 2 trips
- silu · mul, fused
- load gate and up once
- do both operations in registers
- store the answer once
- the intermediate never leaves the chip — it lives in a register for the two instructions that need it
- same maths, same answer, 56 KB less traffic per token per layer

## Step 5 of 11

**Caption.** And you can, because chapter 12 already traced the model. Inductor takes that graph, walks the chain of pointwise ops, and writes a Triton kernel that does all of them in one pass.

**On stage.**

- who writes that kernel
- the traced graph
- chapter 12 already
- has it: a flat list
- of tensor ops
- Inductor
- walk the list, group
- the pointwise ops
- that touch the same rows
- Triton, generated
- one kernel per group,
- compiled at startup,
- cached on disk
- @triton.jit
- def fused_silu_mul(gate, up, out, n):
- i = tl.program_id(0) * BLOCK + tl.arange(0, BLOCK)
- g = tl.load(gate + i) # one read
- u = tl.load(up + i) # one read
- tl.store(out + i, g * tl.sigmoid(g) * u) # one write
- two loads and one store: g * sigmoid(g) never leaves the register it was computed in
- --cuda-graph-tc-compiler inductor
- chapter 12's default was eager: trace, cut, and stop there

## Step 6 of 11

**Caption.** Pointwise ops (output i needs only input i: silu, mul, add) and reductions (a row to one number: norm) fuse into one kernel. A matmul needs whole rows and columns, so Inductor picks a library kernel for it and folds the pointwise edges in.

**On stage.**

- one layer · 11 kernels before, 8 after
- norm
- qkv
- rope
- attn
- o
- add
- norm
- gate·up
- silu
- down
- add
- norm
- qkv·rope
- attn
- o·add
- norm
- gate·up
- silu
- down·add
- pointwise · out[i] depends only on in[i] · silu, mul, add, rope · a chain of them is one pass over the data, so one kernel
- reduction · a row collapses to one number · norm's mean of squares · also one pass, with an accumulator
- matmul · out[i, j] needs all of row i of A and column j of B · not one pass over anything
- written
- by Inductor: one new Triton kernel per pointwise or reduction chain
- 3 of 8
- picked
- from a library: cuBLAS, a Triton template or CUTLASS; its pointwise neighbours fold into it
- 4 of 8
- left eager
- the attention seam from chapter 12
- 1 of 8
- the 3 kernels that went away were pointwise neighbours of matmuls: rope into qkv, each add into the matmul before it

## Step 7 of 11

**Caption.** One idea first. A matmul's output C is carved into tiles, one per thread block. Whatever the tile's shape, its block loads its rows of A and its columns of B in full, so a bigger tile makes more outputs from each load.

**On stage.**

- C = A × B · [8 × 12] = [8 × 6] × [6 × 12] · one tile of C and what its block loads, two ways
- B
- A
- C
- tile
- BLOCK 4 × 4 · 6 tiles, 6 thread blocks
- each block loads 4 rows of A and 4 columns of B
- 48 cells loaded for 16 outputs · 3.0 per output
- whole C: 288 loads for 96 outputs
- B
- A
- C
- tile
- BLOCK 8 × 6 · 2 tiles, 2 thread blocks
- each block loads 8 rows of A and 6 columns of B
- 84 cells loaded for 48 outputs · 1.8 per output
- whole C: 168 loads for 96 outputs
- a tile's block loads its rows of A and its columns of B in full, whatever the tile's shape
- 8 × 6 makes the same 96 outputs from 168 loads instead of 288, holding 48 accumulators per block instead of 16
- but it makes 2 blocks, and an H100 has 132 SMs to keep busy: the right shape depends on C

## Step 8 of 11

**Caption.** At batch 16, C has 16 rows. A square 128 × 128 tile pads 112 of them with zeros, and 14,336 / 128 is only 112 tiles for 132 SMs. A 16 × 64 tile pads nothing and makes 224. So benchmark a few shapes on the real C and keep the fastest.

**On stage.**

- a decode matmul at batch 16 · C = [16 × 14,336] · the first 1,024 columns, one pixel per row
- C · 16 rows
- … to column 14,336
- BLOCK 128 × 128
- 16 of its 128 rows exist; 112 are zero padding
- 7/8 of the tile's arithmetic is on zeros
- 14,336 / 128 = 112 tiles for 132 SMs: 20 sit idle
- BLOCK 16 × 64
- every row it covers is real
- 14,336 / 64 = 224 tiles: every SM busy
- candidate tile shapes · time on that C, shorter is better
- BLOCK 128×128
- 100%
- BLOCK 64×128
- 82%
- BLOCK 32×128
- 66%
- BLOCK 16×256
- 55%
- BLOCK 16×64
- 47%
- keep this one
- a library's default tile is tuned for a square C; below 128 rows it is mostly padding, and a short, wide tile wins

## Step 9 of 11

**Caption.** Fusing that chain removes 470 MB from a decode step at batch 256. Against the 16 GB weight read that is about 3%, which on a memory-bound step is about 140 µs of 4.8 ms.

**On stage.**

- one decode step at batch 256 · weights and activations, then time · chapter 4's KV cache read not shown
- bytes
- weights · 16.0 GB
- activations · 1.4 GB
- 17.4 GB
- 0 GB
- 4 GB
- 8 GB
- 12 GB
- 16 GB
- fusing removes 470 MB · 3% of the weight read
- the step is memory-bound, so bytes ÷ 3.35 TB/s of HBM bandwidth = time
- time
- GPU · 4.8 ms reading the weights
- CPU · 4.2 ms launching · gone since chapters 11 and 12
- 9.0 ms
- 0 ms
- 2 ms
- 4 ms
- 6 ms
- 8 ms
- fusing · 140 µs
- 470 MB ÷ 3.35 TB/s = 140 µs of a 4.8 ms weight read: 3% of the weight read, 3% of its time
- chapters 11 and 12 removed 4.2 ms; this removes about 30× less

## Step 10 of 11

**Caption.** Startup gains jobs, each once per captured shape: compile a kernel per fused group, benchmark tile shapes per matmul (max-autotune only), then record. Recording goes last: it freezes whichever kernels exist when it is taken.

**On stage.**

- startup, before the first request · one row per captured shape, left to right in order
- compile
- 8 fused kernels
- benchmark
- 4 matmuls × 5 tile shapes · max-autotune only
- record
- chapter 11
- batch 1
- batch 2
- batch 4
- batch 8
- batch 16
- = 40
- = 100
- = 5
- not ×32 for the layers: they are identical, so one compile per shape serves all of them
- --torch-compile-max-bs 16
- sets the rows · larger batches run with none of this
- max-autotune
- turns the benchmark column on · off by default, and that column is empty
- measured in SGLang: 90 s on a 235B MoE, 158 s on GLM-5.2, before the first request

## Step 11 of 11

**Caption.** The layer has been cut, fused and recorded — except for one kernel. Attention was cut out in chapter 12 and has run eagerly ever since. So what can be done with attention itself? Chapter 14.

**On stage.**

- one layer, after three chapters of work
- norm
- qkv
- rope
- attn
- o
- add
- norm
- gate·up
- silu
- down
- add
- still eager, still untouched
- the order you met them
- chapter 11
- record
- chapter 12
- cut
- chapter 13
- fuse
- the order they happen
- cut
- fuse
- record
- cut before recording: attention would not capture
- fuse before recording: a recording freezes whichever kernels exist when it is taken
- Attention sat outside all three. So what can be done with the attention kernel itself?

## Prose · The two halves of one tool, and what each is worth

`torch.compile` is a front end and a back end. **Dynamo** is the front end: a bytecode interpreter that traces Python into a straight-line FX graph of tensor operations, breaking the graph wherever it meets something it cannot trace. That is the half chapter 12 used — the graph is what you split at the seams. **Inductor** is the default back end: it fuses pointwise and reduction chains into generated Triton kernels, auto-tunes their block sizes, and for matmuls *selects* a backend (cuBLAS, a Triton template, CUTLASS) with prologue and epilogue fusion around it. Matmuls are not generated from scratch.

The fusion arithmetic, for Llama-3-8B in bf16: SiLU's output is $14336 \times 2 = 28$ KB per token per layer, written to HBM and read straight back by the multiply. Fusing removes both trips, $56$ KB per token per layer, $1.8$ MB per token across 32 layers. At batch 256 that is roughly 470 MB against the step's 16 GB weight read — about 3%, or 140 µs of 4.8 ms. Autotuning is worth more at small batch, where a decode matmul is a sliver against a square-tuned library default; SGLang describes `--enable-torch-compile` as accelerating "small models on small batch sizes", and reported gains sit around 5–15% below batch 16. vLLM reports up to 8% from fused SiLU+quantize on an FP8 405B model, which is the same trick paying better once there is a quantise step to absorb.

In SGLang the standalone `--enable-torch-compile` path is flagged as out of maintenance; the live path is the piecewise one from chapter 12, where `--cuda-graph-tc-compiler inductor` turns the back end on for each captured piece. `max-autotune` benchmarks every candidate for every shape, so compilation costs minutes at startup; `--torch-compile-max-bs` caps how many shapes pay it, and the Inductor cache (`TORCHINDUCTOR_CACHE_DIR`) can be shipped to other machines.
