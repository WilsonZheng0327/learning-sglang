# 09 · One request, end to end

From an HTTP POST to the GPU and back.

Source: `src/pages/ch/09-one-request.mdx` · `src/components/OneRequest.svelte`
Generated by `node scripts/transcript.mjs` — edit the source, not this file.

## Step 1 of 10

**Caption.** Launch the server. It loads the 16 GB of weights onto the GPU, carves the rest of the card into a KV pool, and opens an HTTP port. Then it waits.

**On stage.**

- python -m sglang.launch_server --model-path … --port 30000
- http server
- listening on :30000
- async · thousands of connections
- tokenizer
- text ↔ ids
- the scheduler loop
- GPU · 80 GB
- weights
- KV pool · 64 GB, empty
- waiting list: empty · running: none
- stepping 209× a second, doing nothing yet
- weights loaded once; the KV pool is the 64 GB from chapter 7; the loop from chapter 6 is already spinning

## Step 2 of 10

**Caption.** A client sends a chat request: JSON over HTTP, with stream: true. The server parses it and applies the chat template. Now it has a string, and the GPU can't use a string.

**On stage.**

- one request comes in
- client
- POST /v1/chat/
- completions
- stream: true
- http server
- listening on :30000
- async · thousands of connections
- tokenizer
- text ↔ ids
- the scheduler loop
- GPU · 80 GB
- weights
- KV pool · 64 GB, empty
- waiting list: empty · running: none
- stepping 209× a second, doing nothing yet
- JSON

## Step 3 of 10

**Caption.** Tokenization turns the text into ids, chapter 1's first step. It's CPU work: microseconds for a short prompt, a couple of milliseconds for a long one. Now the ids need to reach the scheduler.

**On stage.**

- one request comes in
- client
- POST /v1/chat/
- completions
- stream: true
- http server
- listening on :30000
- async · thousands of connections
- tokenizer
- "The capital of France is"
- → 791 6864 315 9822 374
- the scheduler loop
- GPU · 80 GB
- weights
- KV pool · 64 GB, empty
- waiting list: empty · running: none
- stepping 209× a second, doing nothing yet
- ids → ?

## Step 4 of 10

**Caption.** Put it all in one process and the scheduler loop shares one Python interpreter with the HTTP server, the tokenizer, and the detokenizer. Python runs one at a time, so every one of those jobs lands between steps while the GPU idles.

**On stage.**

- one request comes in
- client
- POST /v1/chat/
- completions
- stream: true
- http server
- listening on :30000
- async · thousands of connections
- tokenizer
- "The capital of France is"
- → 791 6864 315 9822 374
- the scheduler loop
- GPU · 80 GB
- weights
- KV pool · 64 GB, empty
- waiting list: empty · running: none
- stepping 209× a second, doing nothing yet
- ids → ?
- one process · everything shares one Python interpreter (the GIL) and one core
- the only thread
- GPU
- idle
- idle
- step
- step
- step
- step
- step
- 0 ms
- 10 ms
- 20 ms
- 30 ms
- GPU step
- tokenize
- parse JSON
- detokenize
- write responses
- GPU idle 25% of the time, and the gaps are random: a long prompt to tokenize, a burst of clients to answer
- a 4.8 ms step can't afford to share its interpreter with anything that takes milliseconds

## Step 5 of 10

**Caption.** So SGLang splits the work into three processes: one talks HTTP and tokenizes, one runs the scheduler loop that drives the GPU and nothing else, one turns ids back into text. Three interpreters on three cores; the loop never waits.

**On stage.**

- one request comes in
- client
- POST /v1/chat/
- completions
- stream: true
- http server
- listening on :30000
- async · thousands of connections
- tokenizer
- "The capital of France is"
- → 791 6864 315 9822 374
- the scheduler loop
- GPU · 80 GB
- weights
- KV pool · 64 GB, empty
- waiting list: empty · running: none
- stepping 209× a second, doing nothing yet
- ids → ?
- three processes · three interpreters · three cores · the same work, 24 ms instead of 32
- server + tokenizer
- scheduler + GPU
- detokenizer
- step
- step
- step
- step
- step
- 0 ms
- 5 ms
- 10 ms
- 15 ms
- 20 ms
- GPU step
- tokenize
- parse JSON
- detokenize
- write responses
- the GPU lane is solid: tokenizing the next prompt and detokenizing the last step's tokens happen while the current step runs
- with tensor parallelism there is one scheduler process per GPU (chapter 18); the server and detokenizer stay single

## Step 6 of 10

**Caption.** Three processes need a way to hand things over. ZeroMQ is a queue between processes: the tokenizer drops a message in and moves on. At the top of every step the scheduler takes out whatever has arrived. Neither waits for the other.

**On stage.**

- one request comes in
- client
- POST /v1/chat/
- completions
- stream: true
- http server
- listening on :30000
- async · thousands of connections
- tokenizer
- "The capital of France is"
- → 791 6864 315 9822 374
- the scheduler loop
- GPU · 80 GB
- weights
- KV pool · 64 GB, empty
- waiting list: empty · running: none
- stepping 209× a second, doing nothing yet
- ids → ?
- three processes · three interpreters · three cores · the same work, 24 ms instead of 32
- server + tokenizer
- scheduler + GPU
- detokenizer
- step
- step
- step
- step
- step
- 0 ms
- 5 ms
- 10 ms
- 15 ms
- 20 ms
- GPU step
- tokenize
- parse JSON
- detokenize
- write responses
- the GPU lane is solid: tokenizing the next prompt and detokenizing the last step's tokens happen while the current step runs
- with tensor parallelism there is one scheduler process per GPU (chapter 18); the server and detokenizer stay single
- a queue between two processes
- server + tokenizer
- process 1 · its own loop:
- accept → tokenize → put
- scheduler
- process 2 · its own loop:
- take → step → put
- the zmq socket · a queue
- put
- take
- busy: a step in progress
- request 3 tokenized → put → back to accepting
- process 1 puts and moves on · process 2 takes everything at the top of its next step
- the same shape on every wire: scheduler → detokenizer, detokenizer → server
- a Unix socket underneath, tens of microseconds; messages sit in the sender's queue until the receiver takes them

## Step 7 of 10

**Caption.** Three wires, three queues, each touched by exactly two processes. The same request rides around the outside, changing shape at each hop but keeping its rid. A queue over a Unix socket works over TCP too.

**On stage.**

- one request comes in
- client
- POST /v1/chat/
- completions
- stream: true
- http server
- listening on :30000
- async · thousands of connections
- tokenizer
- "The capital of France is"
- → 791 6864 315 9822 374
- the scheduler loop
- GPU · 80 GB
- weights
- KV pool · 64 GB, empty
- waiting list: empty · running: none
- stepping 209× a second, doing nothing yet
- ids → ?
- three processes · three interpreters · three cores · the same work, 24 ms instead of 32
- server + tokenizer
- scheduler + GPU
- detokenizer
- step
- step
- step
- step
- step
- 0 ms
- 5 ms
- 10 ms
- 15 ms
- 20 ms
- GPU step
- tokenize
- parse JSON
- detokenize
- write responses
- the GPU lane is solid: tokenizing the next prompt and detokenizing the last step's tokens happen while the current step runs
- with tensor parallelism there is one scheduler process per GPU (chapter 18); the server and detokenizer stay single
- a queue between two processes
- server + tokenizer
- process 1 · its own loop:
- accept → tokenize → put
- scheduler
- process 2 · its own loop:
- take → step → put
- the zmq socket · a queue
- put
- take
- busy: a step in progress
- request 3 tokenized → put → back to accepting
- process 1 puts and moves on · process 2 takes everything at the top of its next step
- the same shape on every wire: scheduler → detokenizer, detokenizer → server
- a Unix socket underneath, tens of microseconds; messages sit in the sender's queue until the receiver takes them
- server + tokenizer
- process 1
- scheduler
- process 2
- detokenizer
- process 3
- TokenizedGenerateReqInput
- rid a1f3 · ids […]
- BatchTokenIDOut
- rids […] · ids [12366, …]
- BatchStrOut
- rids […] · [" Paris", …]
- any transport · ipc:// on one machine, tcp:// across machines, same code. Chapter 22.

## Step 8 of 10

**Caption.** Inside the scheduler the message becomes a Req, the object that carries this request from here on. It joins the waiting list, gets its prefill, then a token every step, as chapters 6 to 8 described.

**On stage.**

- one request, all the way through
- client
- POST /v1/chat/
- completions
- stream: true
- process 1 · server
- listening on :30000
- async · thousands of connections
- tokenizer
- "The capital of France is"
- → 791 6864 315 9822 374
- process 2 · scheduler
- GPU · 80 GB
- weights
- KV pool · 64 GB, pages
- waiting → prefill → running → decode
- → one id out, every 4.8 ms
- Req · rid · input ids · output ids · pages · sampling
- process 3 · detokenizer
- 12366 → " Paris" · 13 → "."
- ids
- token ids · zmq
- text · zmq
- three processes · three interpreters · three cores · the same work, 24 ms instead of 32
- server + tokenizer
- scheduler + GPU
- detokenizer
- step
- step
- step
- step
- step
- 0 ms
- 5 ms
- 10 ms
- 15 ms
- 20 ms
- GPU step
- tokenize
- parse JSON
- detokenize
- write responses
- the GPU lane is solid: tokenizing the next prompt and detokenizing the last step's tokens happen while the current step runs
- with tensor parallelism there is one scheduler process per GPU (chapter 18); the server and detokenizer stay single
- a queue between two processes
- server + tokenizer
- process 1 · its own loop:
- accept → tokenize → put
- scheduler
- process 2 · its own loop:
- take → step → put
- the zmq socket · a queue
- put
- take
- busy: a step in progress
- request 3 tokenized → put → back to accepting
- process 1 puts and moves on · process 2 takes everything at the top of its next step
- the same shape on every wire: scheduler → detokenizer, detokenizer → server
- a Unix socket underneath, tens of microseconds; messages sit in the sender's queue until the receiver takes them
- server + tokenizer
- process 1
- scheduler
- process 2
- detokenizer
- process 3
- TokenizedGenerateReqInput
- rid a1f3 · ids […]
- BatchTokenIDOut
- rids […] · ids [12366, …]
- BatchStrOut
- rids […] · [" Paris", …]
- any transport · ipc:// on one machine, tcp:// across machines, same code. Chapter 22.

## Step 9 of 10

**Caption.** The detokenizer turns each id into text, and the server writes it to the client's open connection as a server-sent event, one per token. Words appear as they're decoded.

**On stage.**

- one request, all the way through
- client
- POST /v1/chat/
- completions
- stream: true
- process 1 · server
- listening on :30000
- async · thousands of connections
- tokenizer
- "The capital of France is"
- → 791 6864 315 9822 374
- process 2 · scheduler
- GPU · 80 GB
- weights
- KV pool · 64 GB, pages
- waiting → prefill → running → decode
- → one id out, every 4.8 ms
- Req · rid · input ids · output ids · pages · sampling
- process 3 · detokenizer
- 12366 → " Paris" · 13 → "."
- ids
- token ids · zmq
- text · zmq
- data: {"choices":[{"delta":{"content":" Paris"}}]}
- data: {"choices":[{"delta":{"content":"."}}]}
- ← one server-sent event per token, 4.8 ms apart
- three processes · three interpreters · three cores · the same work, 24 ms instead of 32
- server + tokenizer
- scheduler + GPU
- detokenizer
- step
- step
- step
- step
- step
- 0 ms
- 5 ms
- 10 ms
- 15 ms
- 20 ms
- GPU step
- tokenize
- parse JSON
- detokenize
- write responses
- the GPU lane is solid: tokenizing the next prompt and detokenizing the last step's tokens happen while the current step runs
- with tensor parallelism there is one scheduler process per GPU (chapter 18); the server and detokenizer stay single
- a queue between two processes
- server + tokenizer
- process 1 · its own loop:
- accept → tokenize → put
- scheduler
- process 2 · its own loop:
- take → step → put
- the zmq socket · a queue
- put
- take
- busy: a step in progress
- request 3 tokenized → put → back to accepting
- process 1 puts and moves on · process 2 takes everything at the top of its next step
- the same shape on every wire: scheduler → detokenizer, detokenizer → server
- a Unix socket underneath, tens of microseconds; messages sit in the sender's queue until the receiver takes them
- server + tokenizer
- process 1
- scheduler
- process 2
- detokenizer
- process 3
- TokenizedGenerateReqInput
- rid a1f3 · ids […]
- BatchTokenIDOut
- rids […] · ids [12366, …]
- BatchStrOut
- rids […] · [" Paris", …]
- any transport · ipc:// on one machine, tcp:// across machines, same code. Chapter 22.

## Step 10 of 10

**Caption.** Open connections cost the server process memory, not GPU time. The scheduler runs a few hundred at once; everyone else waits in line, connection open, no tokens yet. That line is the backpressure. Inside the loop: chapter 10.

**On stage.**

- one request, all the way through
- client
- POST /v1/chat/
- completions
- stream: true
- process 1 · server
- listening on :30000
- async · thousands of connections
- tokenizer
- "The capital of France is"
- → 791 6864 315 9822 374
- process 2 · scheduler
- GPU · 80 GB
- weights
- KV pool · 64 GB, pages
- waiting → prefill → running → decode
- → one id out, every 4.8 ms
- Req · rid · input ids · output ids · pages · sampling
- process 3 · detokenizer
- 12366 → " Paris" · 13 → "."
- ids
- token ids · zmq
- text · zmq
- data: {"choices":[{"delta":{"content":" Paris"}}]}
- data: {"choices":[{"delta":{"content":"."}}]}
- ← one server-sent event per token, 4.8 ms apart
- three processes · three interpreters · three cores · the same work, 24 ms instead of 32
- server + tokenizer
- scheduler + GPU
- detokenizer
- step
- step
- step
- step
- step
- 0 ms
- 5 ms
- 10 ms
- 15 ms
- 20 ms
- GPU step
- tokenize
- parse JSON
- detokenize
- write responses
- the GPU lane is solid: tokenizing the next prompt and detokenizing the last step's tokens happen while the current step runs
- with tensor parallelism there is one scheduler process per GPU (chapter 18); the server and detokenizer stay single
- a queue between two processes
- server + tokenizer
- process 1 · its own loop:
- accept → tokenize → put
- scheduler
- process 2 · its own loop:
- take → step → put
- the zmq socket · a queue
- put
- take
- busy: a step in progress
- request 3 tokenized → put → back to accepting
- process 1 puts and moves on · process 2 takes everything at the top of its next step
- the same shape on every wire: scheduler → detokenizer, detokenizer → server
- a Unix socket underneath, tens of microseconds; messages sit in the sender's queue until the receiver takes them
- server + tokenizer
- process 1
- scheduler
- process 2
- detokenizer
- process 3
- TokenizedGenerateReqInput
- rid a1f3 · ids […]
- BatchTokenIDOut
- rids […] · ids [12366, …]
- BatchStrOut
- rids […] · [" Paris", …]
- any transport · ipc:// on one machine, tcp:// across machines, same code. Chapter 22.
- an example load · 3,000 chat tabs open on this server
- 3,000
- open connections
- an assumption for this example
- 2,837 idle · 122 running · 41 queued
- 122
- running
- chapter 7's memory cap at 4k context
- 64 GB ÷ 524 MB = 122 running at most
- 41
- waiting
- submitted this second, not yet admitted
- connection open, no tokens yet
- what one step of the loop carries
- 122 decode tokens · one per running request
- 178-token prefill chunk · next in line
- = 300 tokens · 4.8 ms
- every 4.8 ms: 122 clients each get a token; one waiting client gets part of its prefill
- backpressure
- a burst of requests breaks nothing: 122 run, 41 wait, everyone else holds an open connection and a place in line
- clients feel a longer wait for the first token, not an error; past a configured queue length the server answers 429 instead
- SGLang: --max-running-requests · --max-queued-requests · the line is Scheduler.waiting_queue

## Prose · The moving parts, once

`python -m sglang.launch_server` starts three kinds of process. The HTTP server (FastAPI on uvicorn) and the `TokenizerManager` share one process: it accepts connections, applies the chat template, tokenizes, and owns every open response stream. The `Scheduler` runs in its own process, one per GPU, and does nothing but the loop from chapters 6 to 8. The `DetokenizerManager` turns token ids back into text incrementally.

They talk over ZeroMQ PUSH/PULL sockets on `ipc://` Unix domain sockets: `TokenizedGenerateReqInput` in, `BatchTokenIDOut` to the detokenizer, `BatchStrOut` back to the server. The scheduler calls `recv_pyobj(zmq.NOBLOCK)` in a loop at the top of each step to drain everything that arrived. Streaming responses are server-sent events, one `data:` line per token in the OpenAI-compatible format.
