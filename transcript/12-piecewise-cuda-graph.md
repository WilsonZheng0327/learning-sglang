# 12 · Piecewise CUDA graphs

When the graph has to break.

Source: `src/pages/ch/12-piecewise-cuda-graph.mdx` · `src/components/PiecewiseGraphs.svelte`
Generated by `node scripts/transcript.mjs` — edit the source, not this file.

## Step 1 of 12

**Caption.** Chapter 11 ended here. Decode gets a graph because its batch is always [B, 1]; prefill is ragged, so it runs eagerly — and that was fine, because prefill is compute-bound anyway. That last part deserves a second look.

**On stage.**

- a decode batch
- req 1
- req 2
- req 3
- req 4
- req 5
- shape [B, 1] · only B changes
- a rung per B · one graph for the whole step
- a prefill batch
- seq 1
- seq 2
- seq 3
- shape [Σ tokens, …] · ragged
- no two alike · eager, kernel by kernel
- chapter 11 signed off on that with "prefill is compute-bound anyway, so the launches are hidden"
- which is true of a big prefill. It is not true of a small one.

## Step 2 of 12

**Caption.** Only above about 560 tokens. Below that, the GPU finishes a prefill step in less time than the CPU needs to launch it — prefill is launch-bound, exactly like decode was in chapter 11.

**On stage.**

- one prefill step · time against the number of tokens in it
- 0
- 200
- 400
- 600
- 800
- tokens in the batch
- 0 ms
- 4 ms
- 8 ms
- 12 ms
- launch-bound
- the launches are hidden
- 300 · chapter 4's balance point
- 560
- GPU · the larger of these
- the weight read · 4.8 ms
- compute · 16 µs a token
- the launches · 9.0 ms
- below 560 tokens the step costs what the CPU takes to launch it — the same wall decode hit in chapter 11
- a short prompt, the tail chunk of a chunked prefill, a vision encoder: all of them live down there

## Step 3 of 12

**Caption.** So record prefill too. The token count changes every batch, but chapter 11 already solved that: a ladder of sizes and pad up to the next rung. Same trick, a different axis — tokens instead of requests.

**On stage.**

- three prefill batches in a row · the token count is different every time
- batch 1
- 56 real
- +8
- → the 64 graph
- batch 2
- 131 real
- +13
- → the 144 graph
- batch 3
- 92 real
- +4
- → the 96 graph
- chapter 11's ladder was requests; this one is tokens. Pad up, replay the rung, drop the padding's output.
- so far so good — and this is not enough

## Step 4 of 12

**Caption.** Except padding fixes the total, not the boundaries. These two batches are both 56 tokens, and attention has to do completely different work in each: every token attends only back to the start of its own sequence.

**On stage.**

- two prefill batches · both 56 tokens · what attention has to compute in each
- batch 1 · 18 + 27 + 11
- keys, in order →
- queries
- ↓
- 3 triangles · 18×18 · 27×27 · 11×11
- batch 2 · 40 + 9 + 7
- keys, in order →
- queries
- ↓
- 3 triangles · 40×40 · 9×9 · 7×7
- every token attends back only to the start of its own sequence, so the boundaries are part of the work
- padding gave both batches the same shape. It could not give them the same attention.

## Step 5 of 12

**Caption.** So run both batches through a layer and compare. 10 of the 11 kernels do the identical thing, because all they ever see is a row count. Only attention changes — and only attention reads the boundaries.

**On stage.**

- one layer, run on each of them · what actually changes between the two?
- batch 1 · 18+27+11
- batch 2 · 40+9+7
- same?
- norm
- [56, 4096]
- [56, 4096]
- ✓
- qkv
- [56, 4096] × [4096, 6144]
- [56, 4096] × [4096, 6144]
- ✓
- rope
- [56, 4096]
- [56, 4096]
- ✓
- attn
- 18×18 27×27 11×11
- 40×40 9×9 7×7
- ✗
- o
- [56, 4096] × [4096, 4096]
- [56, 4096] × [4096, 4096]
- ✓
- add
- [56, 4096]
- [56, 4096]
- ✓
- norm
- [56, 4096]
- [56, 4096]
- ✓
- gate·up
- [56, 4096] × [4096, 28672]
- [56, 4096] × [4096, 28672]
- ✓
- silu
- [56, 14336]
- [56, 14336]
- ✓
- down
- [56, 14336] × [14336, 4096]
- [56, 14336] × [14336, 4096]
- ✓
- add
- [56, 4096]
- [56, 4096]
- ✓
- 10 rows of 11 are identical: same kernel, same shapes, same addresses — one recording replays for both batches
- the row that differs is the only one that has to know where the sequences start

## Step 6 of 12

**Caption.** So cut there. 32 attentions means 32 cuts and 33 pieces, roughly one layer each. Every piece is token-wise, so every piece can be recorded at a padded token count. Attention is left out.

**On stage.**

- one prefill step · 32 layers · cut at every attention
- embed
- lm_head
- 32 attentions · 32 cuts
- what's left between the cuts
- piece 1
- piece 2
- piece 3
- piece 4
- piece 5
- piece 6
- piece 7
- piece 8
- …
- 33 pieces, roughly one layer each · every one of them token-wise, so every one can be recorded at a padded token count
- 33 recordings per rung instead of one — that is the whole idea

## Step 7 of 12

**Caption.** A step is now an alternation: replay a piece, run attention eagerly, replay the next piece. The CPU wakes up 32 times instead of once — and stays asleep for the 904 kernels in between.

**On stage.**

- one prefill step, replayed piecewise · time runs left to right
- CPU
- GPU
- replay
- piece 1
- eager
- attn
- replay
- piece 2
- eager
- attn
- replay
- piece 3
- eager
- attn
- replay
- piece 4
- eager
- attn
- replay
- piece 5
- eager
- attn
- …
- …
- the CPU is idle inside every orange block and busy only at the seams
- 33 replays and 32 eager attentions: 129 things for the CPU to do, not 1,000
- attention is still launched the old way, and it is the one kernel big enough not to care

## Step 8 of 12

**Caption.** 9.0 ms of CPU becomes about 1.2 ms: 33 replays plus the attention launches. That is 60× more CPU than one whole-model graph, and it does not matter. You don't need one graph. You need the CPU under the GPU.

**On stage.**

- one prefill step, replayed piecewise · time runs left to right
- CPU
- GPU
- replay
- piece 1
- eager
- attn
- replay
- piece 2
- eager
- attn
- replay
- piece 3
- eager
- attn
- replay
- piece 4
- eager
- attn
- replay
- piece 5
- eager
- attn
- …
- …
- the CPU is idle inside every orange block and busy only at the seams
- 33 replays and 32 eager attentions: 129 things for the CPU to do, not 1,000
- attention is still launched the old way, and it is the one kernel big enough not to care
- CPU time for one prefill step · the GPU needs 4.8 ms of it at small token counts
- eager
- 9.0 ms
- 1,000 launches × 9 µs
- piecewise
- 1.2 ms
- 33 replays (330 µs) + 32 eager attentions (864 µs)
- one whole-model graph
- 20 µs
- not available here: the shape changes every batch
- the GPU's 4.8 ms
- anything left of this line is free
- You don't need one graph. You need the CPU under the GPU.
- piecewise costs 60× what a whole-model graph costs and buys the same thing, because both are under 4.8 ms

## Step 9 of 12

**Caption.** The rungs are token counts now, and the gap grows with the count: 4 apart at the bottom, 256 at the top. Every band holds it near a sixteenth of the batch, so what padding wastes is a share of the work rather than a fixed number of tokens.

**On stage.**

- one prefill step, replayed piecewise · time runs left to right
- CPU
- GPU
- replay
- piece 1
- eager
- attn
- replay
- piece 2
- eager
- attn
- replay
- piece 3
- eager
- attn
- replay
- piece 4
- eager
- attn
- replay
- piece 5
- eager
- attn
- …
- …
- the CPU is idle inside every orange block and busy only at the seams
- 33 replays and 32 eager attentions: 129 things for the CPU to do, not 1,000
- attention is still launched the old way, and it is the one kernel big enough not to care
- CPU time for one prefill step · the GPU needs 4.8 ms of it at small token counts
- eager
- 9.0 ms
- 1,000 launches × 9 µs
- piecewise
- 1.2 ms
- 33 replays (330 µs) + 32 eager attentions (864 µs)
- one whole-model graph
- 20 µs
- not available here: the shape changes every batch
- the GPU's 4.8 ms
- anything left of this line is free
- You don't need one graph. You need the CPU under the GPU.
- piecewise costs 60× what a whole-model graph costs and buys the same thing, because both are under 4.8 ms
- the gap between rungs, against the size of the batch · SGLang's default schedule
- 4
- 32
- 256
- 1024
- 4096
- tokens in the batch
- 4
- 16
- 64
- 256
- gap
- every 4
- 4–32
- every 16
- 48–256
- every 32
- 288–512
- every 64
- 576–1024
- every 256
- 1280–4096
- above 32 tokens the gap stays under a sixteenth of the batch
- so padding costs a fixed share of the work, never a fixed number of tokens
- 50 rungs in all
- 33 recordings each
- built once at startup

## Step 10 of 12

**Caption.** Attention isn't the only seam. All-reduce waits on other GPUs (chapter 18); MoE dispatch sends a data-dependent number of tokens to each expert. The rule is the same: if its work depends on the data rather than the token count, cut there.

**On stage.**

- one prefill step, replayed piecewise · time runs left to right
- CPU
- GPU
- replay
- piece 1
- eager
- attn
- replay
- piece 2
- eager
- attn
- replay
- piece 3
- eager
- attn
- replay
- piece 4
- eager
- attn
- replay
- piece 5
- eager
- attn
- …
- …
- the CPU is idle inside every orange block and busy only at the seams
- 33 replays and 32 eager attentions: 129 things for the CPU to do, not 1,000
- attention is still launched the old way, and it is the one kernel big enough not to care
- CPU time for one prefill step · the GPU needs 4.8 ms of it at small token counts
- eager
- 9.0 ms
- 1,000 launches × 9 µs
- piecewise
- 1.2 ms
- 33 replays (330 µs) + 32 eager attentions (864 µs)
- one whole-model graph
- 20 µs
- not available here: the shape changes every batch
- the GPU's 4.8 ms
- anything left of this line is free
- You don't need one graph. You need the CPU under the GPU.
- piecewise costs 60× what a whole-model graph costs and buys the same thing, because both are under 4.8 ms
- the gap between rungs, against the size of the batch · SGLang's default schedule
- 4
- 32
- 256
- 1024
- 4096
- tokens in the batch
- 4
- 16
- 64
- 256
- gap
- every 4
- 4–32
- every 16
- 48–256
- every 32
- 288–512
- every 64
- 576–1024
- every 256
- 1280–4096
- above 32 tokens the gap stays under a sixteenth of the batch
- so padding costs a fixed share of the work, never a fixed number of tokens
- 50 rungs in all
- 33 recordings each
- built once at startup
- what gets cut out · and the one question behind all of it
- attention
- needs the sequence boundaries, which change every batch
- all-reduce
- waits on the other GPUs in the group · chapter 18
- MoE dispatch
- how many tokens go to each expert is decided by the tokens
- does its work depend on the data, or only on the token count?
- only the token count
- pad it, record it, put it in a piece
- the data
- leave it out; it becomes a seam
- the seam list is configuration, not a law: an engine names the ops it wants to cut at

## Step 11 of 12

**Caption.** Nobody writes 33 capture regions by hand. torch.compile traces the model into a graph of operations; you name the ops to split on, it cuts there and hands back the subgraphs. Each one gets the chapter 11 treatment.

**On stage.**

- one prefill step, replayed piecewise · time runs left to right
- CPU
- GPU
- replay
- piece 1
- eager
- attn
- replay
- piece 2
- eager
- attn
- replay
- piece 3
- eager
- attn
- replay
- piece 4
- eager
- attn
- replay
- piece 5
- eager
- attn
- …
- …
- the CPU is idle inside every orange block and busy only at the seams
- 33 replays and 32 eager attentions: 129 things for the CPU to do, not 1,000
- attention is still launched the old way, and it is the one kernel big enough not to care
- CPU time for one prefill step · the GPU needs 4.8 ms of it at small token counts
- eager
- 9.0 ms
- 1,000 launches × 9 µs
- piecewise
- 1.2 ms
- 33 replays (330 µs) + 32 eager attentions (864 µs)
- one whole-model graph
- 20 µs
- not available here: the shape changes every batch
- the GPU's 4.8 ms
- anything left of this line is free
- You don't need one graph. You need the CPU under the GPU.
- piecewise costs 60× what a whole-model graph costs and buys the same thing, because both are under 4.8 ms
- the gap between rungs, against the size of the batch · SGLang's default schedule
- 4
- 32
- 256
- 1024
- 4096
- tokens in the batch
- 4
- 16
- 64
- 256
- gap
- every 4
- 4–32
- every 16
- 48–256
- every 32
- 288–512
- every 64
- 576–1024
- every 256
- 1280–4096
- above 32 tokens the gap stays under a sixteenth of the batch
- so padding costs a fixed share of the work, never a fixed number of tokens
- 50 rungs in all
- 33 recordings each
- built once at startup
- what gets cut out · and the one question behind all of it
- attention
- needs the sequence boundaries, which change every batch
- all-reduce
- waits on the other GPUs in the group · chapter 18
- MoE dispatch
- how many tokens go to each expert is decided by the tokens
- does its work depend on the data, or only on the token count?
- only the token count
- pad it, record it, put it in a piece
- the data
- leave it out; it becomes a seam
- the seam list is configuration, not a law: an engine names the ops it wants to cut at
- how 33 capture regions get found without anyone writing them down
- 1 · the model
- a Python class,
- a forward() that
- calls other modules
- 2 · trace it
- run it once with
- Dynamo watching;
- record every op
- 3 · cut
- at the named ops:
- attention, all-reduce,
- MoE dispatch
- 4 · capture
- 33 subgraphs,
- each recorded per
- rung — chapter 11
- what the tracer hands back
- norm
- qkv
- rope
- ✂
- attn
- o
- add
- norm
- gate·up
- silu
- down
- add
- ✂
- attn
- …
- a flat list of operations, in order, with the seams marked — the graph was always there; nobody had written it down
- SGLang wraps the model in torch.compile for exactly this, then replaces each subgraph with a replayable piece

## Step 12 of 12

**Caption.** The matmuls have to touch memory: that's where the weights are. The two in the middle touch it only because of how they were called — and the tracer handed them back untouched. Chapter 13.

**On stage.**

- one prefill step, replayed piecewise · time runs left to right
- CPU
- GPU
- replay
- piece 1
- eager
- attn
- replay
- piece 2
- eager
- attn
- replay
- piece 3
- eager
- attn
- replay
- piece 4
- eager
- attn
- replay
- piece 5
- eager
- attn
- …
- …
- the CPU is idle inside every orange block and busy only at the seams
- 33 replays and 32 eager attentions: 129 things for the CPU to do, not 1,000
- attention is still launched the old way, and it is the one kernel big enough not to care
- CPU time for one prefill step · the GPU needs 4.8 ms of it at small token counts
- eager
- 9.0 ms
- 1,000 launches × 9 µs
- piecewise
- 1.2 ms
- 33 replays (330 µs) + 32 eager attentions (864 µs)
- one whole-model graph
- 20 µs
- not available here: the shape changes every batch
- the GPU's 4.8 ms
- anything left of this line is free
- You don't need one graph. You need the CPU under the GPU.
- piecewise costs 60× what a whole-model graph costs and buys the same thing, because both are under 4.8 ms
- the gap between rungs, against the size of the batch · SGLang's default schedule
- 4
- 32
- 256
- 1024
- 4096
- tokens in the batch
- 4
- 16
- 64
- 256
- gap
- every 4
- 4–32
- every 16
- 48–256
- every 32
- 288–512
- every 64
- 576–1024
- every 256
- 1280–4096
- above 32 tokens the gap stays under a sixteenth of the batch
- so padding costs a fixed share of the work, never a fixed number of tokens
- 50 rungs in all
- 33 recordings each
- built once at startup
- what gets cut out · and the one question behind all of it
- attention
- needs the sequence boundaries, which change every batch
- all-reduce
- waits on the other GPUs in the group · chapter 18
- MoE dispatch
- how many tokens go to each expert is decided by the tokens
- does its work depend on the data, or only on the token count?
- only the token count
- pad it, record it, put it in a piece
- the data
- leave it out; it becomes a seam
- the seam list is configuration, not a law: an engine names the ops it wants to cut at
- how 33 capture regions get found without anyone writing them down
- 1 · the model
- a Python class,
- a forward() that
- calls other modules
- 2 · trace it
- run it once with
- Dynamo watching;
- record every op
- 3 · cut
- at the named ops:
- attention, all-reduce,
- MoE dispatch
- 4 · capture
- 33 subgraphs,
- each recorded per
- rung — chapter 11
- what the tracer hands back
- norm
- qkv
- rope
- ✂
- attn
- o
- add
- norm
- gate·up
- silu
- down
- add
- ✂
- attn
- …
- a flat list of operations, in order, with the seams marked — the graph was always there; nobody had written it down
- SGLang wraps the model in torch.compile for exactly this, then replaces each subgraph with a replayable piece
- one piece, up close · the token-wise chain the tracer handed back unchanged
- CPU
- asleep
- GPU
- the arithmetic happens up here, in registers on the SM
- gate·up
- read
- write
- silu
- mul
- down
- HBM
- the GPU's own 80 GB — the weights and the KV cache are in here too
- 4 kernels, 8 trips — and every arrow is inside the GPU. The CPU sent one replay and went back to sleep.
- a kernel's registers are gone the moment it ends, so whatever the next one needs has to go out to memory and come back
- ×32 layers, and again for every token in the batch
- Nothing but mul ever reads what silu wrote, and it wants it immediately. So why are they two kernels?

## Prose · Where the numbers come from

A prefill step of $n$ tokens costs the GPU $\max(t_{\text{mem}},\; n \cdot t_{\text{tok}})$ — the 4.8 ms weight read from chapter 4, or the compute, whichever is larger. The CPU's side is flat: about a thousand launches at 9 µs each, whatever rides along. So the step is launch-bound while $9\ \text{ms} > n \cdot t_{\text{tok}}$, which for an 8B model at roughly a petaflop is $n < 560$ — the same crossover chapter 11 found on the batch-size axis, because it is the same 9 ms on the other side of the comparison.

Piecewise replaces those launches with one replay per piece plus eager attention: $33 \times 10\,\mu s + 32 \times 3 \times 9\,\mu s \approx 1.2$ ms. Sixty times what a single whole-model graph costs, and irrelevant, because the target was never zero — it was 4.8 ms.

A kernel's registers and shared memory do not survive the kernel, and two kernels are two independent launches with no state in common, so a value passing from one to the next has to be written to global memory and read back. That memory is HBM — the GPU's own DRAM, the same place the weights and the KV cache live; the CPU is not involved at any point. (At small sizes some of those reads are caught by the 50 MB L2 rather than reaching HBM, but a prefill-sized activation — `[2048, 14336]` in bf16 is 58 MB — is well past that.)

SGLang builds this with `torch.compile`: `PiecewiseCudaGraphRunner` wraps the model to trigger Dynamo tracing, splits the FX graph at the ops named as split points, and replaces each capturable subgraph with a backend that captures and replays it per rung. `--cuda-graph-tc-compiler` picks what the compiler does with the pieces — `eager` (the default) traces and splits only; `inductor` also generates kernels, which is chapter 13. The prefill rung schedule (`--cuda-graph-bs-prefill`) defaults to 4–32 by 4, 48–256 by 16, 288–512 by 32, 576–1024 by 64, 1280–4096 by 256, then by 512.
